apiVersion: monitoring.coreos.com/v1
kind: Alertmanager
metadata:
  name: alertmanager
  namespace: default
spec:
  alertmanagerConfigSelector:
    matchLabels:
      alertmanagerConfig: example
  replicas: 1
  resources:
    requests:
      memory: 200Mi
