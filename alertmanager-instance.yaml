apiVersion: monitoring.coreos.com/v1
kind: Alertmanager
metadata:
  name: alertmanager
  namespace: default
spec:
  alertManagerSelector:
    matchLabels:
      
  replicas: 1
  resources:
    requests:
      memory: 200Mi
